{
  "name": "action-google-access-token",
  "version": "1.0.0",
  "description": "GitHub action to acquire an access token for a Google API service account",
  "main": "lib/main.js",
  "scripts": {
    "build": "tsc && ncc build"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.36.1",
    "typescript": "^4.8.4"
  },
  "dependencies": {
    "@actions/core": "^1.10.0",
    "googleapis": "^108.0.0"
  }
}
